# Kanban PRD Manager Configuration

# Anthropic API Key (required for Claude chat)
ANTHROPIC_API_KEY=your-api-key-here

# Target repository to manage (absolute path)
# This is the repo where agents will create worktrees and make changes
TARGET_REPO=/path/to/your/project

# Server port
PORT=4000

# Maximum agent iterations (Ralph Wiggum loop limit)
MAX_ITERATIONS=50

# Timeout per iteration in milliseconds (default: 30 minutes)
TIMEOUT_MS=1800000

# Path to Claude CLI (if not in PATH)
CLAUDE_PATH=claude
